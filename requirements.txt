# Generated by pip freeze — do not edit manually.
# Source constraints are in requirements.in
#
# To regenerate: pip freeze > requirements.txt (then verify against requirements.in)

# Core Web Framework
fastapi==0.109.0
uvicorn==0.42.0
pydantic==2.12.5
pydantic-settings==2.13.1

# Database
asyncpg==0.31.0
psycopg2-binary==2.9.9
SQLAlchemy==2.0.35
# alembic — not currently installed

# Redis
redis==7.3.0
# hiredis — not currently installed

# Task Queue
# celery — not currently installed
# flower — not currently installed

# SSH and Security
paramiko==4.0.0
cryptography==46.0.3
PyJWT==2.12.1
bcrypt==3.2.0
python-multipart==0.0.22

# LLM Providers
anthropic==0.86.0
# claude-agent-sdk — installed but not pinnable via pip freeze
mcp==1.26.0
fastmcp==3.1.1
openai==2.29.0
aiohttp==3.13.3

# Monitoring and Logging
structlog==25.5.0
python-json-logger==2.0.7
# sentry-sdk — not currently installed
prometheus-client==0.19.0
lmnr==0.7.42

# Data Processing
pandas==2.2.3
numpy==2.1.2
openpyxl==3.1.5
XlsxWriter==3.2.3
vanna==2.0.2

# Supabase
# supabase — not currently installed

# Rate Limiting
# slowapi — not currently installed

# Utils
python-dotenv==1.2.2
click==8.3.1
httpx==0.28.1
tenacity==9.1.4
cachetools==7.0.5

# Development
pytest==9.0.2
pytest-asyncio==1.3.0
# pytest-cov — not currently installed
# pytest-json-report — not currently installed
# black — not currently installed
# flake8 — not currently installed
# ruff — not currently installed
# mypy — not currently installed
# pre-commit — not currently installed

# PDF Generation
reportlab==3.6.8
# markdown2 — not currently installed

# Database Drivers (optional, based on client needs)
# pymysql — not currently installed
# pyodbc — not currently installed
# oracledb — not currently installed
# scipy — not currently installed
# statsmodels — not currently installed
dlt==1.24.0

# ── Transitive dependencies (auto-resolved) ──────────────────────────────────
annotated-types==0.6.0
anyio==4.12.1
certifi==2024.2.2
charset-normalizer==3.3.2
distro==1.7.0
et_xmlfile==2.0.0
exceptiongroup==1.3.1
h11==0.14.0
httpcore==1.0.8
httptools==0.6.1
idna==3.3
iniconfig==2.3.0
jiter==0.13.0
MarkupSafe==2.0.1
pluggy==1.6.0
pydantic_core==2.41.5
PyNaCl==1.5.0
pytz==2026.1.post1
PyYAML==6.0.3
sniffio==1.3.0
starlette==0.52.1
typing_extensions==4.15.0
uvloop==0.19.0
watchfiles==1.1.1
