[pytest]
asyncio_mode = auto
testpaths = tests security
addopts = -v --tb=short
markers =
    mandatory: tests that must pass before any merge (pipeline core)
    live: tests that call real LLM agents via proxy (slow, non-deterministic)
